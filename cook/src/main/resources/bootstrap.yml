spring:
  application:
    name: cook
  cloud:
    service-registry:
      auto-registration:
        fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOSTNAME:localhost}:8761/eureka/